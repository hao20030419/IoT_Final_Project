"""
VISUAL OVERVIEW - Two-Stage Video Recognition System
====================================================
"""

# SYSTEM ARCHITECTURE

┌─────────────────────────────────────────────────────────────┐
│          TWO-STAGE VIDEO RECOGNITION SYSTEM               │
└─────────────────────────────────────────────────────────────┘

                         INPUT VIDEO
                             ↓
                    ┌────────────────┐
                    │ Read Frames    │
                    │ (OpenCV)       │
                    └────────────────┘
                             ↓
             ┌───────────────────────────────┐
             ▼                               ▼
      ┌─────────────┐              ┌──────────────┐
      │ Stage 1:    │              │ Stage 1:     │
      │ Frame Diff  │              │ Frame Diff   │
      │ Detection   │              │ Detection    │
      │ (5ms)       │              │ (5ms)        │
      └─────────────┘              └──────────────┘
             │                            │
        Motion? → YES                 Motion? → NO
             │                            │
             ▼                            ▼
      ┌─────────────┐              ┌──────────────┐
      │ Stage 2:    │              │ Skip YOLO    │
      │ YOLO        │              │ (Save Time)  │
      │ Detection   │              │              │
      │ (100ms)     │              │              │
      └─────────────┘              └──────────────┘
             │                            │
             └───────────────┬────────────┘
                             ▼
                   ┌──────────────────┐
                   │ Output Results   │
                   │ - Detections     │
                   │ - Metrics        │
                   │ - Speedup        │
                   └──────────────────┘


# SPEEDUP VISUALIZATION

Traditional Full YOLO:
┌──────┬──────┬──────┬──────┬──────┐  Total: 500ms
│ 100ms│ 100ms│ 100ms│ 100ms│ 100ms│  5 frames
└──────┴──────┴──────┴──────┴──────┘

Two-Stage Processing (40% motion):
┌──────┬───┬───────┬───┬───────┬───┐  Total: 200ms
│static│FD ║motion║FD ║motion ║FD │  3 frames
│ 5ms  │ 5ms║100ms ║ 5ms║100ms ║ 5ms│
└──────┴───┴───────┴───┴───────┴───┘

Speedup: 500ms ÷ 200ms = 2.5x faster!


# GUI LAYOUT

┌─────────────────────────────────────────────────────────┐
│  Two-Stage Video Recognition System                     │
├─────────────────────────────────────────────────────────┤
│ Control Panel                          [Progress: 50%]  │
│ [Upload] [Process] [Stop]                               │
├──────────────────────────┬──────────────────────────────┤
│                          │                              │
│  VIDEO DISPLAY           │  RESULTS & METRICS          │
│  ┌────────────────────┐  │  ┌──────────────────────┐   │
│  │                    │  │  │ Frame Info:          │   │
│  │  🔴 DETECTED       │  │  │ Frame: 123/1000      │   │
│  │  Motion indicator  │  │  │ Status: Motion       │   │
│  │  (Red/Green)       │  │  │ Objects: 3           │   │
│  │                    │  │  │ - person (0.95)      │   │
│  │  + YOLO boxes      │  │  │ - car (0.87)         │   │
│  │  + Labels          │  │  │                      │   │
│  │                    │  │  ├──────────────────────┤   │
│  └────────────────────┘  │  │ Performance Metrics: │   │
│                          │  │ Speedup: 2.5x        │   │
│  Playback Controls:      │  │ Time Saved: 150s     │   │
│  [▶ Play] [⏸ Pause]    │  │ Reduction: 60%       │   │
│  Frame Slider: [=====> ]  │  │                      │   │
│  100/1000                │  └──────────────────────┘   │
│                          │                              │
└──────────────────────────┴──────────────────────────────┘


# PROCESSING PIPELINE

VIDEO INPUT
    ↓
[Frame Difference Detection]
    ├─→ Grayscale Conversion
    ├─→ Gaussian Blur (21×21)
    ├─→ Calculate Difference
    ├─→ Apply Threshold
    └─→ Count Changed Pixels
        ↓
    Motion > Threshold?
        ├─→ YES: Continue to YOLO
        │   ↓
        │   [YOLOv8 Detection]
        │   ├─→ Load Nano Model
        │   ├─→ Run Inference
        │   ├─→ Extract Boxes
        │   ├─→ Get Classes
        │   └─→ Return Detections
        │
        └─→ NO: Skip YOLO (Save Time!)
            ↓
    [Combine Results]
        ├─→ Draw Bounding Boxes
        ├─→ Add Status Text
        └─→ Store Frame
            ↓
    [Output]
        ├─→ Processed Frame
        ├─→ Detection Results
        └─→ Timing Info


# SPEEDUP FORMULA BREAKDOWN

Given a 1-minute video (1800 frames at 30fps):

Full YOLO Approach:
─────────────────────────────────
Total Time = Frames × YOLO Time/Frame
           = 1800 × 100ms
           = 180,000ms
           = 3 minutes

Two-Stage Approach (40% motion):
─────────────────────────────────
Total Time = (Static Frames × FD Time) + (Motion Frames × YOLO Time)
           = (1080 × 5ms) + (720 × 100ms)
           = 5,400ms + 72,000ms
           = 77,400ms
           = 1.3 minutes

Speedup Factor:
───────────────
Speedup = 180,000ms ÷ 77,400ms = 2.33x

Speedup Percentage:
───────────────────
Reduction = (180,000 - 77,400) / 180,000 × 100
          = 102,600 / 180,000 × 100
          = 57%

Time Saved:
───────────
180 seconds - 77.4 seconds = 102.6 seconds (1.7 minutes!)


# FEATURE HIERARCHY

┌─────────────────────────────────────────┐
│    Two-Stage Video Recognition         │
├─────────────────────────────────────────┤
│                                         │
├─ CORE FEATURES                         │
│  ├─ Frame Difference Detection          │
│  ├─ YOLOv8 Object Detection             │
│  └─ Speedup Calculation                 │
│                                         │
├─ GUI FEATURES                          │
│  ├─ Video Upload                        │
│  ├─ Real-time Processing                │
│  ├─ Video Playback                      │
│  ├─ Motion Status Display                │
│  ├─ Object Detection Display            │
│  └─ Metrics Dashboard                   │
│                                         │
├─ ADVANCED FEATURES                     │
│  ├─ Configuration Presets                │
│  ├─ GPU Acceleration Support            │
│  ├─ Batch Processing Capability        │
│  └─ Custom Thresholds                   │
│                                         │
├─ DOCUMENTATION                         │
│  ├─ Comprehensive README                │
│  ├─ Installation Guide (Multi-OS)      │
│  ├─ Performance Analysis                │
│  ├─ Quick Reference Guide               │
│  └─ Project Summary                     │
│                                         │
└─────────────────────────────────────────┘


# PERFORMANCE MATRIX

┌───────────────────┬──────────┬──────────┬──────────────┐
│ Video Type        │ Motion % │ Speedup  │ Use Case     │
├───────────────────┼──────────┼──────────┼──────────────┤
│ Surveillance      │ 10-20%   │ 8-15x    │ CCTV, Security│
│ Presentation      │ 30-40%   │ 2.5-3.5x │ Screen Record│
│ General Video     │ 40-60%   │ 1.8-2.5x │ Movies       │
│ Action Movie      │ 70-90%   │ 1.2-1.5x │ High Motion  │
└───────────────────┴──────────┴──────────┴──────────────┘


# DATA FLOW

     User Input
         ↓
    [Video Upload]
         ↓
    Select File
         ↓
    [Process Click]
         ↓
    ┌─────────────────┐
    │ Background      │
    │ Processing      │
    │ Thread          │
    └─────────────────┘
         ↓
    [Two-Stage Run] ← Progress updates to GUI
         ↓
    [Full YOLO Run] ← Progress updates to GUI
         ↓
    [Calculate Speedup]
         ↓
    [Display Results]
         ↓
    [Enable Playback]
         ↓
    [User Review]


# FILE DEPENDENCIES

main_gui.py
├── depends on: video_processor.py
├── depends on: tkinter (built-in)
├── depends on: PIL
├── depends on: cv2
└── depends on: threading (built-in)

video_processor.py
├── depends on: frame_difference.py
├── depends on: yolo_detector.py
├── depends on: cv2
└── depends on: time (built-in)

frame_difference.py
├── depends on: cv2
└── depends on: numpy

yolo_detector.py
├── depends on: ultralytics (YOLOv8)
├── depends on: cv2
└── depends on: numpy


# CONFIGURATION OPTIONS

┌──────────────────────┬──────────┬──────┬────────────────┐
│ Preset               │ Threshold│ Model│ Confidence     │
├──────────────────────┼──────────┼──────┼────────────────┤
│ Fast (Speed)         │ 3000     │ nano │ 0.6            │
│ Balanced (Default)   │ 5000     │ nano │ 0.5            │
│ Accurate (Quality)   │ 7000     │ small│ 0.4            │
│ Realtime (Live)      │ 2000     │ nano │ 0.7            │
└──────────────────────┴──────────┴──────┴────────────────┘


# INSTALLATION SUMMARY

Step 1: Dependencies
─────────────────────
pip install -r requirements.txt
(Takes 5-10 minutes on first run)

Step 2: Verification
────────────────────
python setup_check.py
(Confirms all packages installed)

Step 3: Demo (Optional)
───────────────────────
python demo.py
(Test with sample videos)

Step 4: Application
───────────────────
python main_gui.py
(Launch the GUI)


# OUTPUT METRICS EXPLANATION

Speedup Factor (e.g., 2.5x)
───────────────────────────
Means: Two-stage is 2.5 times faster than full YOLO
Calculation: Full YOLO Time ÷ Two-Stage Time

Speedup Percentage (e.g., 60%)
──────────────────────────────
Means: 60% faster than full YOLO
Calculation: (Full YOLO Time - Two-Stage Time) ÷ Full YOLO Time × 100

Time Saved (e.g., 120 seconds)
──────────────────────────────
Means: Saved 2 minutes compared to full YOLO
Calculation: Full YOLO Time - Two-Stage Time

YOLO Reduction (e.g., 60%)
──────────────────────────
Means: Skipped 60% of YOLO operations
Calculation: (1 - YOLO Runs ÷ Total Frames) × 100


# QUICK PERFORMANCE EXPECTATIONS

Fast Processing (GPU, Nano Model):
──────────────────────────────────
1-minute video:    30-60 seconds
10-minute video:   300-600 seconds (5-10 minutes)

Moderate Processing (CPU, Nano Model):
──────────────────────────────────────
1-minute video:    1-2 minutes
10-minute video:   10-20 minutes

High Quality (GPU, Small Model):
────────────────────────────────
1-minute video:    1-2 minutes
10-minute video:   10-20 minutes


─────────────────────────────────────────────────────────
Version 1.0 | December 2024 | Python 3.8+ | All Platforms
─────────────────────────────────────────────────────────
